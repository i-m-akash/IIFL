# Leave empty in dev: Vite proxies `/api` → http://127.0.0.1:8787 (run `wrangler dev` in DynamicBackend).
# VITE_API_BASE_URL=https://your-worker.workers.dev

# Optional: voice playground WebSocket (same as legacy Mobicule frontend).
# VITE_WS_URL=wss://your-playground.example/ws
